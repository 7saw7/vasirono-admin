import type { NotificationFilterOption } from "@/features/backoffice/notifications/types";
export function NotificationTypesTable({types}:{types:NotificationFilterOption[]}){return <section className="space-y-3"><div><h2 className="text-lg font-semibold text-neutral-950">Tipos registrados</h2><p className="mt-1 text-xs text-neutral-500">Catálogo expuesto por Notifications Service.</p></div>{types.length?<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{types.map(type=><div key={type.value} className="rounded-xl border border-neutral-200 bg-white p-4 shadow-sm"><p className="text-xs text-neutral-500">ID {type.value}</p><p className="mt-1 text-sm font-semibold text-neutral-900">{type.label}</p></div>)}</div>:<div className="rounded-xl border border-dashed border-neutral-300 bg-white p-5 text-sm text-neutral-500">No hay tipos de notificación disponibles.</div>}</section>}
